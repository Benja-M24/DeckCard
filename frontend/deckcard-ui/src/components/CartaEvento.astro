---
import type { CartaEvento } from "../models/types";

interface Props {
  carta: CartaEvento;
}

const { carta } = Astro.props;
---

<div class="carta-evento">
  <div class="p-4">
    <h2 class="text-2xl font-bold mb-4 text-green-600">{carta.titulo}</h2>
    <div class="flex justify-center flex-col lg:flex-row gap-6">
      <div class="flex-1 flex flex-col items-center">
        <div
          class="bg-green-100 p-4 rounded-lg shadow-md w-full max-w-xs mb-4"
        >
          {
            carta.imagen && (
              <img
                src={carta.imagen}
                alt={carta.titulo}
                class="w-full h-auto object-cover rounded-md border-2 border-green-300 shadow-md mb-4"
              />
            )
          }
          <div class="border-2 border-green-400 p-6 rounded-md">
            <h3 class="text-xl font-semibold mb-2 text-center">
              {carta.titulo}
            </h3>
            <p class="text-sm mb-4 italic text-center">❝{carta.frase}❞</p>
            <div class="flex justify-center">
              <span class="inline-block bg-green-200 text-green-800 text-xs px-2 py-1 rounded-full">
                Evento
              </span>
            </div>
          </div>
        </div>
      </div>
      <div class="flex-1 flex flex-col gap-3">
        <div>
          <h3 class="text-md text-center font-bold mb-1">Consignas de Juego</h3>
          <ul
            class="list-disc pl-5 text-sm text-gray-700 bg-gray-50 p-2 rounded-md"
          >
            {carta.consignas_juego.map((consigna) => <li>{consigna}</li>)}
          </ul>
        </div>
        <div class="mt-4 bg-green-50 p-3 rounded-md shadow-sm">
          <p class="text-sm">
            Versión: <span class="font-semibold">#{carta.version}</span>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
